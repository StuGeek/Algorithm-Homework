#include "Deque.h"

using namespace std;

bool Deque::empty() const {
    return empty();
}

int Deque::size() const {
    return size();
}

void Deque::pop_front() {
    pop_front();
}

void Deque::pop_rear() {
    pop_back();
}

void Deque::push_front(const T &item) {
    push_front(item);
}

void Deque::push_rear(const T &item) {
    push_back(item);
}

const T &Deque::front() const {
    return front();
}

const T &Deque::rear() const {
    return rear();
}

T &Deque::front() {
    return front();
}

T &Deque::rear() {
    return rear();
}